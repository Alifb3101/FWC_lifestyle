
  # Brand Name Creation

  This is a code bundle for Brand Name Creation. The original project is available at https://www.figma.com/design/rkcE44fl0rhV7um852RsO1/Brand-Name-Creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  